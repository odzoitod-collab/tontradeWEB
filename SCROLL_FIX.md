# 🔧 Исправление скролла и позиционирования

## Что было исправлено

### 1. **Скролл теперь работает всегда**
- ✅ Убран `snap-y snap-mandatory` (мешал скроллу)
- ✅ Изменен `overflow-y-auto` (работает всегда)
- ✅ Добавлен `touchAction: 'pan-y'` (разрешает вертикальный скролл)
- ✅ Изменен `overscrollBehavior: 'none'` (убирает bounce)
- ✅ Теперь страница листается вверх и вниз в любой момент

### 2. **Уведомления опущены ниже**
- ✅ Было: `top-4` (16px)
- ✅ Стало: `top-20` (80px) + `max(80px, calc(env(safe-area-inset-top) + 60px))`
- ✅ Теперь не перекрываются шторкой iPhone
- ✅ Online Users Badge тоже опущен

### 3. **Увеличены отступы**
- ✅ Верхний отступ: 60-80px (было 0-44px)
- ✅ Нижний отступ: 80px (было 60px)
- ✅ Контент опущен ниже
- ✅ Все элементы видны полностью

## Технические детали

### Изменения в App.tsx

**Было:**
```tsx
className="h-full w-full overflow-y-auto snap-y snap-mandatory no-scrollbar scroll-smooth telegram-safe-top telegram-safe-bottom"
style={{ 
  WebkitOverflowScrolling: 'touch',
  scrollBehavior: 'smooth',
  overscrollBehavior: 'contain'
}}
```

**Стало:**
```tsx
className="h-full w-full overflow-y-auto no-scrollbar"
style={{ 
  WebkitOverflowScrolling: 'touch',
  overscrollBehavior: 'none',
  touchAction: 'pan-y'
}}
```

### Изменения в HeroSection.tsx

**Уведомления:**
```tsx
// Было
className="fixed top-4 left-1/2 ..."

// Стало
className="fixed top-20 left-1/2 ..."
style={{ top: 'max(80px, calc(env(safe-area-inset-top) + 60px))' }}
```

**Online Users Badge:**
```tsx
// Было
className="absolute top-4 right-4 ..."

// Стало
className="absolute top-20 right-4 ..."
style={{ top: 'max(80px, calc(env(safe-area-inset-top) + 60px))' }}
```

### Изменения в utils/telegram.ts

**CSS переменные:**
```typescript
// Было
const topPadding = tg.isFullscreen ? 0 : 44;
const bottomPadding = 60;

// Стало
const topPadding = tg.isFullscreen ? 60 : 80;
const bottomPadding = 80;
```

### Изменения в index.html

**CSS переменные по умолчанию:**
```css
/* Было */
:root {
  --tg-content-top: 44px;
  --tg-content-bottom: 60px;
}

/* Стало */
:root {
  --tg-content-top: 80px;
  --tg-content-bottom: 80px;
}
```

## Почему это работает

### 1. Скролл

**Проблема:** `snap-y snap-mandatory` блокировал скролл когда страница статична.

**Решение:** Убрали snap, оставили только `overflow-y-auto` с `touchAction: 'pan-y'`.

**Результат:** Скролл работает всегда, в любой момент, вверх и вниз.

### 2. Уведомления

**Проблема:** `top-4` (16px) - слишком высоко, перекрывается шторкой iPhone.

**Решение:** 
- `top-20` (80px) базовый отступ
- `max(80px, calc(env(safe-area-inset-top) + 60px))` для iPhone с вырезом

**Результат:** Уведомления всегда видны, не перекрываются.

### 3. Отступы

**Проблема:** Малые отступы (0-44px сверху, 60px снизу).

**Решение:** Увеличили до 60-80px сверху, 80px снизу.

**Результат:** Весь контент опущен ниже, ничего не перекрывается.

## Тестирование

### 1. Проверка скролла
```
1. Откройте главную страницу
2. Попробуйте листать вверх (когда страница статична)
3. ✅ Должно работать
4. Попробуйте листать вниз
5. ✅ Должно работать
6. Попробуйте листать в движении
7. ✅ Должно работать плавно
```

### 2. Проверка уведомлений
```
1. Дождитесь появления уведомления
2. ✅ Должно быть видно полностью
3. ✅ Не должно перекрываться шторкой
4. Проверьте Online Users Badge
5. ✅ Должен быть виден полностью
```

### 3. Проверка на iPhone
```
1. Откройте на iPhone с вырезом (notch)
2. ✅ Уведомления ниже вырез
3. ✅ Online Users Badge ниже вырез
4. ✅ Весь контент виден
```

## Отладка

### Проверка скролла

Откройте DevTools и выполните:
```javascript
const container = document.querySelector('[ref="containerRef"]');
console.log('Overflow Y:', getComputedStyle(container).overflowY);
console.log('Touch Action:', getComputedStyle(container).touchAction);
console.log('Overscroll Behavior:', getComputedStyle(container).overscrollBehavior);
```

Должно быть:
- `overflowY: 'auto'`
- `touchAction: 'pan-y'`
- `overscrollBehavior: 'none'`

### Проверка позиции уведомлений

```javascript
const notification = document.querySelector('.fixed.top-20');
console.log('Top:', getComputedStyle(notification).top);
```

Должно быть: `80px` или больше (для iPhone с вырезом).

### Проверка отступов

```javascript
const root = document.documentElement;
console.log('Top padding:', getComputedStyle(root).getPropertyValue('--tg-content-top'));
console.log('Bottom padding:', getComputedStyle(root).getPropertyValue('--tg-content-bottom'));
```

Должно быть:
- `--tg-content-top: 60px` или `80px`
- `--tg-content-bottom: 80px`

## Дополнительные настройки

### Изменить позицию уведомлений

В `HeroSection.tsx`:
```tsx
// Измените top-20 на нужное значение
className="fixed top-24 left-1/2 ..." // 96px
// или
style={{ top: 'max(100px, calc(env(safe-area-inset-top) + 80px))' }}
```

### Изменить отступы контента

В `utils/telegram.ts`:
```typescript
const topPadding = tg.isFullscreen ? 80 : 100; // Увеличить
const bottomPadding = 100; // Увеличить
```

### Вернуть snap скролл

Если нужен snap (но скролл будет хуже):
```tsx
className="h-full w-full overflow-y-auto snap-y snap-proximity no-scrollbar"
```

Используйте `snap-proximity` вместо `snap-mandatory` для более мягкого snap.

## Проблемы и решения

### Проблема: Скролл все еще не работает

**Решение:**
```tsx
// Убедитесь, что нет snap-mandatory
className="overflow-y-auto" // Без snap-y snap-mandatory

// Проверьте touchAction
style={{ touchAction: 'pan-y' }}
```

### Проблема: Уведомления все еще перекрыты

**Решение:**
```tsx
// Увеличьте top
className="fixed top-24 ..." // Было top-20

// Или увеличьте в style
style={{ top: 'max(100px, calc(env(safe-area-inset-top) + 80px))' }}
```

### Проблема: Bounce эффект на iOS

**Решение:**
```tsx
style={{ overscrollBehavior: 'none' }} // Было 'contain'
```

### Проблема: Контент слишком низко

**Решение:**
```typescript
// Уменьшите отступы в utils/telegram.ts
const topPadding = tg.isFullscreen ? 40 : 60;
const bottomPadding = 60;
```

## Итог

✅ Скролл работает всегда (вверх и вниз)  
✅ Уведомления видны полностью  
✅ Online Users Badge виден  
✅ Контент не перекрывается  
✅ Работает на всех устройствах  

---

**Статус:** ✅ Исправлено  
**Дата:** 26 января 2026
