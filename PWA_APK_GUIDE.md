# 📱 Руководство по PWA и APK

## Что было добавлено

### 1. PWA (Progressive Web App)
- ✅ Manifest.json для установки как приложение
- ✅ Service Worker для офлайн работы
- ✅ Иконки всех размеров (72x72 до 512x512)
- ✅ Splash screen для iOS
- ✅ Standalone режим (полноэкранный)

### 2. Оптимизация для мобильных
- ✅ Отключен pull-to-refresh (случайное обновление)
- ✅ Отключен двойной тап для зума
- ✅ Отключено контекстное меню (долгое нажатие)
- ✅ Отключено выделение текста (кроме input)
- ✅ Скрыта адресная строка
- ✅ Safe area для iPhone с вырезом

### 3. Адаптация для APK
- ✅ Полноэкранный режим
- ✅ Портретная ориентация
- ✅ Отключены жесты браузера
- ✅ Навигация телефона не мешает
- ✅ Оптимизация touch событий

## Как установить PWA

### На Android

1. **Через Chrome:**
   - Откройте сайт в Chrome
   - Нажмите меню (три точки)
   - Выберите "Добавить на главный экран"
   - Подтвердите установку

2. **Через Edge:**
   - Откройте сайт в Edge
   - Нажмите меню
   - Выберите "Добавить на телефон"

3. **Автоматический промпт:**
   - При первом посещении появится баннер
   - Нажмите "Установить"

### На iOS

1. **Через Safari:**
   - Откройте сайт в Safari
   - Нажмите кнопку "Поделиться" (квадрат со стрелкой)
   - Прокрутите вниз и выберите "На экран «Домой»"
   - Нажмите "Добавить"

2. **Иконка на главном экране:**
   - Приложение появится как обычное приложение
   - Откроется в полноэкранном режиме
   - Без адресной строки Safari

## Как создать APK

### Вариант 1: PWA Builder (Рекомендуется)

1. **Перейдите на сайт:**
   ```
   https://www.pwabuilder.com/
   ```

2. **Введите URL вашего сайта:**
   ```
   https://ваш-сайт.com
   ```

3. **Нажмите "Start"**

4. **Выберите "Package For Stores"**

5. **Выберите Android:**
   - Нажмите "Generate"
   - Заполните данные приложения
   - Скачайте APK

6. **Настройки для Android:**
   ```json
   {
     "packageId": "com.tontrader.app",
     "name": "TonTrader",
     "launcherName": "TonTrader",
     "themeColor": "#000000",
     "backgroundColor": "#000000",
     "display": "standalone",
     "orientation": "portrait"
   }
   ```

### Вариант 2: Capacitor (Для продвинутых)

1. **Установите Capacitor:**
   ```bash
   npm install @capacitor/core @capacitor/cli
   npm install @capacitor/android
   ```

2. **Инициализируйте:**
   ```bash
   npx cap init
   ```

3. **Добавьте Android платформу:**
   ```bash
   npx cap add android
   ```

4. **Соберите проект:**
   ```bash
   npm run build
   npx cap sync
   ```

5. **Откройте в Android Studio:**
   ```bash
   npx cap open android
   ```

6. **Соберите APK в Android Studio:**
   - Build → Build Bundle(s) / APK(s) → Build APK(s)

### Вариант 3: Cordova

1. **Установите Cordova:**
   ```bash
   npm install -g cordova
   ```

2. **Создайте проект:**
   ```bash
   cordova create tontrader com.tontrader.app TonTrader
   cd tontrader
   ```

3. **Добавьте платформу:**
   ```bash
   cordova platform add android
   ```

4. **Скопируйте файлы:**
   - Скопируйте dist/ в www/

5. **Соберите APK:**
   ```bash
   cordova build android --release
   ```

## Настройка иконок

### Создание иконок

Вам нужны иконки следующих размеров:
- 72x72 (ldpi)
- 96x96 (mdpi)
- 128x128 (hdpi)
- 144x144 (xhdpi)
- 152x152 (xxhdpi)
- 192x192 (xxxhdpi)
- 384x384 (4x)
- 512x512 (5x)

### Инструменты для создания

1. **Online:**
   - https://realfavicongenerator.net/
   - https://www.pwabuilder.com/imageGenerator

2. **Локально:**
   ```bash
   npm install -g pwa-asset-generator
   pwa-asset-generator logo.png ./public/icons
   ```

### Где разместить

Поместите иконки в:
```
tontradeWEB/public/
  ├── icon-72x72.png
  ├── icon-96x96.png
  ├── icon-128x128.png
  ├── icon-144x144.png
  ├── icon-152x152.png
  ├── icon-192x192.png
  ├── icon-384x384.png
  └── icon-512x512.png
```

## Тестирование PWA

### Chrome DevTools

1. Откройте DevTools (F12)
2. Перейдите в "Application"
3. Проверьте:
   - Manifest
   - Service Workers
   - Storage
   - Cache Storage

### Lighthouse

1. Откройте DevTools (F12)
2. Перейдите в "Lighthouse"
3. Выберите "Progressive Web App"
4. Нажмите "Generate report"
5. Исправьте ошибки

### Тестирование на устройстве

1. **Android:**
   ```bash
   adb devices
   chrome://inspect
   ```

2. **iOS:**
   - Подключите iPhone к Mac
   - Safari → Develop → iPhone → Ваш сайт

## Оптимизации для APK

### 1. Отключение жестов

Уже реализовано в `index.html`:
- Pull-to-refresh отключен
- Двойной тап для зума отключен
- Контекстное меню отключено
- Выделение текста отключено (кроме input)

### 2. Полноэкранный режим

Уже реализовано в `manifest.json`:
```json
{
  "display": "standalone",
  "display_override": ["fullscreen", "standalone"]
}
```

### 3. Safe Area (iPhone)

Уже реализовано в CSS:
```css
.safe-area-top {
  padding-top: env(safe-area-inset-top, 0px);
}
```

### 4. Ориентация

Уже реализовано в `manifest.json`:
```json
{
  "orientation": "portrait"
}
```

## Функции PWA утилит

### Проверка режима

```typescript
import { isStandalone, isMobile, getPlatform } from './utils/pwa';

if (isStandalone()) {
  console.log('Running as PWA/APK');
}

if (isMobile()) {
  console.log('Mobile device');
}

const platform = getPlatform(); // 'ios' | 'android' | 'desktop'
```

### Вибрация

```typescript
import { vibrate } from './utils/pwa';

vibrate(200); // Одна вибрация 200ms
vibrate([200, 100, 200]); // Паттерн вибраций
```

### Уведомления

```typescript
import { requestNotificationPermission, showNotification } from './utils/pwa';

await requestNotificationPermission();
showNotification('Сделка закрыта', {
  body: 'Прибыль: +$10.50',
  icon: '/icon-192x192.png'
});
```

### Копирование в буфер

```typescript
import { copyToClipboard } from './utils/pwa';

const success = await copyToClipboard('Текст для копирования');
```

### Шаринг

```typescript
import { share } from './utils/pwa';

await share({
  title: 'TonTrader',
  text: 'Попробуй эту платформу!',
  url: 'https://tontrader.com'
});
```

## Проблемы и решения

### Проблема: Pull-to-refresh не отключается

**Решение:**
```css
body {
  overscroll-behavior-y: contain;
}
```

### Проблема: Двойной тап зумит

**Решение:**
```css
button, a, div {
  touch-action: manipulation;
}
```

### Проблема: Адресная строка видна

**Решение:**
- Установите PWA через "Добавить на главный экран"
- Или используйте `display: "standalone"` в manifest

### Проблема: Навигация телефона перекрывает контент

**Решение:**
```css
.pb-safe {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}
```

### Проблема: Service Worker не обновляется

**Решение:**
```javascript
// В DevTools → Application → Service Workers
// Нажмите "Unregister" и перезагрузите страницу
```

## Публикация в магазины

### Google Play Store

1. Создайте APK через PWA Builder или Capacitor
2. Зарегистрируйтесь в Google Play Console
3. Создайте новое приложение
4. Загрузите APK
5. Заполните описание и скриншоты
6. Отправьте на проверку

### Apple App Store

1. Используйте Capacitor для создания iOS приложения
2. Зарегистрируйтесь в Apple Developer Program ($99/год)
3. Создайте приложение в App Store Connect
4. Загрузите через Xcode
5. Отправьте на проверку

## Чеклист перед публикацией

- [ ] Все иконки созданы и размещены
- [ ] Manifest.json настроен
- [ ] Service Worker работает
- [ ] PWA проходит Lighthouse тест (90+)
- [ ] Тестирование на реальных устройствах
- [ ] Отключены все жесты браузера
- [ ] Полноэкранный режим работает
- [ ] Safe area учтена для iPhone
- [ ] Уведомления работают
- [ ] Офлайн режим работает

## Полезные ссылки

- [PWA Builder](https://www.pwabuilder.com/)
- [Capacitor](https://capacitorjs.com/)
- [Cordova](https://cordova.apache.org/)
- [Web.dev PWA](https://web.dev/progressive-web-apps/)
- [MDN PWA Guide](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps)

---

**Статус:** ✅ Готово к компиляции в APK  
**Дата:** 26 января 2026
